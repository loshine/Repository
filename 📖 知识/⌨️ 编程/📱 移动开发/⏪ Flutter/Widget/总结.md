# Overview

Flutter 中万物基于 Widget

## 单 child Widget

![single](https://i.imgur.com/O9jkf7S.png)

![single-by-category](https://i.imgur.com/opvfvGx.png)

## 多 children Widget

![multi](https://i.imgur.com/zZOkFlY.png)

## 建议

1. 功能越少的控件，效率越高；
2. ListView 以及 GridView 的 builder 构造函数效率更高；
3. 实现起来比较复杂的控件，效率一般会更高。


if (response.isSuccess()) {
                UserInfo data = response.getData();
                if (data != null) {
                    if (FlagValue.mUserInfo.is_standard != 1 && !("2".equals(data.type) || "2".equals(FlagValue.mUserInfo.type) || "3".equals(data.is_attention))) {
                        CommunityExaminationDialog.show(getSupportFragmentManager());
                    } else {
                        Intent intent = ActivityUtil.getInstance().turnToChatActivity(this, data.member_id, -1, null, 0, 0, null);
                        intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                        startActivity(intent);
                    }
                }
            }